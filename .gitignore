.DS_Store
.py
.json
merge.py
# Vite/Node
node_modules/
dist/
*.local

# Lock files (choose one based on your package manager)
package-lock.json
# or
yarn.lock
# or
pnpm-lock.yaml

# Optional: if you want to keep dev config out of repo
/public
package.json 
vite.config.js
app_docs/


# React subs (#267): build manifests MUST be committed so CI `npm ci && npm run build`
# can run. These negate the global package.json / package-lock.json ignores above.
# (dist/ and node_modules/ stay ignored.)
!subs/storage/package.json
!subs/storage/package-lock.json

CLAUDE.md
.env.example
scripts/backfill_tasks.py
scripts/seed_epics.py
scripts/*
lambda_creation_log.txt
fail-log.txt
