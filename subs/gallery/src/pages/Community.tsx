import { SectionView } from '../components/SectionView';

export default function Community() {
  return (
    <SectionView
      section="community"
      heading={{ title: '🌟 Community Displays', subtitle: 'Holiday displays submitted by the SpookyDecs community' }}
    />
  );
}
