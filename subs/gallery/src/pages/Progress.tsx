import { SectionView } from '../components/SectionView';

export default function Progress() {
  return (
    <SectionView
      section="progress"
      heading={{ title: '🛠️ Custom Builds', subtitle: 'Behind-the-scenes looks at our custom display builds' }}
    />
  );
}
